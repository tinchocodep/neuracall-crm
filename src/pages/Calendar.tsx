import ComingSoon from '../components/common/ComingSoon';
import { Calendar as CalendarIcon } from 'lucide-react';

export default function Calendar() {
    return <ComingSoon title="Calendario" icon={CalendarIcon} />;
}
